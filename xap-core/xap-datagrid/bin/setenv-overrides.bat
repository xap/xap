@echo off
rem *******************************************************************************************************
rem * Use this script to override default GigaSpaces settings using environment variables.                *
rem * For more information see https://docs.gigaspaces.com/15.0/started/common-environment-variables.html *
rem *******************************************************************************************************

rem The directory in which Java is installed
rem set JAVA_HOME=
rem Extra java options and system properties to be used by GigaSpaces
rem set GS_OPTIONS_EXT=
rem Extra classpath to be used by GigaSpaces
rem set GS_CLASSPATH_EXT=
rem The network interface card / host name / host address that will be used by GigaSpaces (defaults to hostname)
rem set GS_NIC_ADDRESS=
rem Lookup groups used for multicast discovery (defaults to version identifier)
rem set GS_LOOKUP_GROUPS=
rem Lookup locators used for unicast discovery
rem set GS_LOOKUP_LOCATORS=
